#pragma once

// Placeholder header for Resolver to satisfy includes if used externally later.
// Full implementation is embedded as a local class in AppGateway.cpp for now,
// to keep the initial deliverable small and buildable per task scope.

