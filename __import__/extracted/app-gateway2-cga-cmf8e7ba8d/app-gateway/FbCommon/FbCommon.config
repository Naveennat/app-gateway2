set(autostart ${PLUGIN_FBCOMMON_AUTOSTART})

if(PLUGIN_FBCOMMON_STARTUPORDER)
set (startuporder ${PLUGIN_FBCOMMON_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_FBCOMMON_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)
