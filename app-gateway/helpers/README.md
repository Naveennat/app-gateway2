Helper headers for app-gateway2 component.

This directory contains project-local helper headers such as:
- WsManager.h: WebSocket connection manager
- UtilsLogging.h: Lightweight logging macros

These are included by various components (AppGateway, LaunchDelegate, etc).
