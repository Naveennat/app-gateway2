#include "BadgerMetricsDelegate.h"

// This translation unit intentionally left minimal.
// The BadgerMetricsDelegate is header-only; this TU exists to satisfy build systems
// that scan for .cpp files per component.
