set (autostart ${PLUGIN_XVPCLIENT_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.XvpClient")

if(PLUGIN_XVPCLIENT_STARTUPORDER)
set (startuporder ${PLUGIN_XVPCLIENT_STARTUPORDER})
endif()

