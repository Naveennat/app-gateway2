set (autostart ${PLUGIN_FBMETRICS_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.FbMetrics")

if(PLUGIN_FBMETRICS_STARTUPORDER)
set (startuporder ${PLUGIN_FBMETRICS_STARTUPORDER})
endif()

