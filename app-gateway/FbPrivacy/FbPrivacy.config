set (autostart ${PLUGIN_FBPRIVACY_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.FbPrivacy")

if(PLUGIN_FBPRIVACY_STARTUPORDER)
set (startuporder ${PLUGIN_FBPRIVACY_STARTUPORDER})
endif()

