#pragma once

// PUBLIC_INTERFACE
// Minimal local stub for <interfaces/definitions.h> to satisfy includes from helpers.
// Add common using/aliases needed by WebSocket helpers.

#include <plugins/Module.h>

// Provide basic aliases often used by helpers
namespace WPEFramework {
namespace Exchange {
// No content required for current build; serves only as presence stub.
}
}
