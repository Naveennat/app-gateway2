set (autostart ${PLUGIN_LAUNCHDELEGATE_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.LaunchDelegate")

if(PLUGIN_LAUNCHDELEGATE_STARTUPORDER)
set (startuporder ${PLUGIN_LAUNCHDELEGATE_STARTUPORDER})
endif()

